%dw 2.0
output application/json
//These 3 dashes are the seperator between the declarations section (above) and body of your DataWeave code (below)
---
//This is where you need to specify the DataWeave needed to transform the inbound payload into the desired output by following the challenge instructions
payload
